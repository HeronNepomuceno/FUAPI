---
const {i} = Astro.props;
const {fillColor = 'rgb(255, 255, 255)', w = '24px', gapRight = ''} = Astro.props;
---

<svg class="icon" style={`fill:${fillColor}; width:${w}; margin-right:${gapRight}`} viewBox="0 0 24 24">
	<use xlink:href={`../sprite.svg#${i}`}></use>
</svg>